{
  "name": "web-research-agent",
  "version": "1.0.0",
  "description": "An AI research agent on Cloudflare: Llama 3.3 on Workers AI + a Workflow that plans, searches the web, and synthesizes a cited report, with per-session memory in an Agents-SDK Durable Object.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "agents": "^0.17.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260629.1",
    "typescript": "^5.6.3",
    "wrangler": "^4.105.0"
  }
}
